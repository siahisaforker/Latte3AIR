#include "game_bin.h"

// Paste the array itself here
unsigned char game_bin[] = {
    0x00, 0x01, 0x02, /* ... ROM bytes ... */
};

// Length of the array
unsigned int game_bin_len = sizeof(game_bin);
