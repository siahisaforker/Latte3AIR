// PlatformNetwork: kept intentionally empty because implementations are inline
// in PlatformNetwork.h to avoid adding build system entries. This file exists
// so existing project layouts that reference it won't break edits.
